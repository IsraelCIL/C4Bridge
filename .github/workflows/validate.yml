name: Validate C4Bridge

on:
  push:
    branches: ["main", "dev/**"]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Python tools
        run: pip install -r requirements-dev.txt

      - name: Install Lua 5.1
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1

      - name: Check Lua syntax
        run: find driver -name '*.lua' -print0 | xargs -0 -n1 luac5.1 -p

      - name: Run driver tests
        run: lua5.1 driver/tests/run.lua

      - name: Check API spec against the driver
        run: python scripts/check_api.py

      - name: Build driver
        run: python scripts/build.py

      - name: Validate package
        run: python scripts/check_package.py

      - name: Show package checksums
        run: (cd dist && sha256sum C4Bridge.c4z openapi.json)

      - name: Check live responses against the API spec
        run: python scripts/check_contract.py

      - name: Validate web app
        run: python scripts/check_web.py

      - name: Check web JavaScript syntax
        run: for file in web/*.js; do node --check "$file"; done

      - name: Run web tests (offline service worker)
        run: node --test tests/web/*.test.mjs
